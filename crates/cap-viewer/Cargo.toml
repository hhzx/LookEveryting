[package]
name = "cap-viewer"
description = "3D model viewport rendering for LookEveryting"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cap-model = { path = "../cap-model" }
cap-ui = { path = "../cap-ui" }
egui.workspace = true
glam = { version = "0.29", default-features = false, features = ["std"] }
